# AGENTS.md

## 项目定位
这是“到站北京”网页端静态原型，不做微信小程序工程。实现方式为纯 HTML/CSS/JS，移动端手机尺寸展示。

## 开工前必读
- docs/page-map.md
- docs/content-inventory.md
- docs/working-notes.md

## 内容规则
- 老师原界面中的页面层级、功能目标、可点击关系和中文文案要尽量保留。
- 不使用 OCR、位图转 CSV、位图自动识别类工具提取文案。页面文字只能通过人工视觉读图后整理。
- 站点图标优先复用 `extracted_page04_station_icons/` 中的原始图标。
- 当前阶段以导出截图作为文字事实源，优先保证页面完整和点击层级准确。除非用户明确要求，不要凭记忆改写截图中的文案。

## 视觉规则
- 做成精修后的政务/交通服务移动网页原型，不复刻原图中过重的 AI 感。
- 避免毛玻璃、强渐变、漂浮装饰、大圆角气泡、随机彩色卡片、无意义插画。
- 使用统一 token、统一卡片、统一列表、统一按钮和统一底栏。

## 交互规则
- 旅客端与司机端都要完整可点。
- 站区切换页需要有“确认更改”按钮。
- 站区切换页底部多余的站名胶囊不保留。
- 表单和业务动作只做原型反馈，不接真实数据，不真实提交。
- 当前 `app.js` 采用“截图页 + 透明热区”实现页面流；后续重构真实 HTML 组件时，必须先对照 `docs/requirements-checklist.md`。
