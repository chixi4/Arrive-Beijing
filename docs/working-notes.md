# Working Notes

## 2026-05-23

- 已确认直接在 `/Users/yuookie/Documents/dev/Arrive-Beijing` 初始化 git。
- 当前目录初始内容为：PDF、48 张导出界面截图、10 个站点图标，未包含前端工程。
- 实现目标为纯 HTML/CSS/JS 静态原型，移动端手机尺寸展示。
- 页面文案不使用 OCR 或自动位图识别工具，后续只根据视觉读图手工整理。
- 站点图标来自 `extracted_page04_station_icons/`，这些图标保留。
- “站区切换页”新增底部确认按钮，并移除原稿中底部多余的站名胶囊。
- 交互范围：旅客端和司机端全做；所有 tab、分类、列表项、服务入口要可点击；不接真实数据、不真实提交表单。
- 用户进一步明确：当前阶段 UI 精修不是重点，页面覆盖率、点击层级和文字完整准确优先级最高。
- 为保证“文字丝毫不差”，当前原型切换为“源截图保真页 + 透明点击热区”模式：除站点选择/站区切换这种明确要求修改交互的页面外，其它页面直接使用老师给出的导出截图作为事实源。
- 站点选择/站区切换页使用导出的站点图标重建；切换页实现横向滚动选择，删除底部多余站名胶囊，并保留“确认更改”按钮。
- `#/services` 已从截图热区页迁移为真实 HTML 左右分栏目录，左侧保留原稿里的分类层级，右侧把交通出行、机场服务、到站北京畅行服务三组内容拆开；旅客端和司机端入口现在直接跳转到对应路由，其余条目先用原型提示承接。
- 这轮又把更底层的业务框架切到了新的 `ab-*` 页面底座：`#/station/home`、`#/feedback/submit`、`#/feedback/mine`、`#/profile`、`#/driver/profile` 都改成了真实 HTML 长页，不再依赖旧截图壳。
- 重新核对用户的基础描述后，修正司机端上层入口：`#/driver/splash` 点击后进入 `#/driver/short-haul/booking`，与首页第 8 个“短途复载”入口一致。
- 为说明中明确要求“都可以点”的局部选项增加可点击选中反馈：路线楼层、公告分类、反馈类型、问题分类、预约日期、预约时段、今日餐饮加减数量。
- 投诉建议上传图片区域按用户建议覆盖为单个“添加”入口，不再呈现 4 个添加图片格子。
- 新增 `docs/page-spec.md`，记录截图覆盖、自定义页面、选项交互和后续真实 HTML 重构提醒。
- 已完成 8 张风格锚点的纯文字生图，最终文件落在 `assets/style-anchors/`。
- 首页和车站排队页各经历过一轮提示词修正：首页收紧为政务交通工具风，车站排队页补全了 10 个站名。
- 2026-05-23 补齐并落盘了本轮位图资产：站区切换页、导航/交通/停车/短途复载页面锚点，以及站点建筑图、开屏主视觉、停车/司机/空状态等业务图。
- 站点建筑图统一从生成的 10 站点总板裁切得到；总板与各业务拼图源图保存在 `assets/reference/generated-sheets/`，便于后续继续迭代或重切。
- 停车与司机两组拼图不是等高四宫格，最终按真实页面断点分别切在 `y=1120` 和 `y=565`，避免把上一屏尾巴带进下半屏。
- 本轮进入像素保真和功能核查阶段：继续保留“截图事实源 + 透明热区”的方式作为参考页保真基线，优先修补真实交互暴露出的断点。
- 修正站区切换页的状态重置问题：点击其他站点后不再被重新盖回当前站点，确认更改后写入 `localStorage` 并回到首页。
- 首页顶端站名增加动态覆盖层：默认北京西站时保持原截图不覆盖，切换到其他站点后覆盖顶部站名胶囊，让换站操作有可见反馈。
- 为避免本地浏览器缓存旧脚本，`index.html` 中的 `styles.css` 与 `app.js` 引用增加版本号。
- 使用 Chrome headless/CDP 做自动核查：47 个页面路由全部可达，45 个截图路由图片加载完成，2 个自定义站点页均保留 10 个站点和确认按钮。
- 静态链接关系检查通过：47 个 `data-to` 目标全部能落到已实现路由，没有死链。
- 关键交互抽查通过：入口流、首次选站确认、站区切换确认、导航楼层、公告分类、反馈类型/分类、单个上传添加、短途复载预约时段、今日餐饮加减数量均能触发原型反馈。
- 新增 `#/style-anchor/02-list` 到 `#/style-anchor/08-modal` 的真实 HTML/CSS 复刻页，作为 18x9 设计锚点的可运行版本。
- 新增 `scripts/style_anchor_check.py`，会把复刻页截图并与 `assets/style-anchors/variants/18x9/` 逐张做像素差异统计。
- 当前 18x9 比对结果：`02-list` 4.99%，`03-detail` 7.19%，`04-form` 3.31%，`05-completion` 2.62%，`06-loading` 2.85%，`07-profile` 3.61%，`08-modal` 3.38%。
- 02 页已经压进 5% 线内；03 页仍是当前最大偏差项，主要还在文字渲染和局部位置的边缘差异。
- 这 7 张复刻页已经可以作为统一设计 token 的共同来源，下面单独抽了一份规范文档。
- 桌面浏览器预览增加了固定比例缩放：当窗口足够宽时，style-anchor 页面会按设计画布比例缩到可视区域内，避免在大屏里拉得过开。
- 进一步修正锚点页的桌面预览画布：`#app.anchor-app` 固定为 1:2 画布，`anchor-page` 统一吃满整屏高度，避免 05/07/08 在桌面窗口里只显示半页或底部空白过多。
- 07 个人中心页的用户卡片与退出登录按钮补齐满宽约束，避免按钮按内容收缩后出现右侧空档。
- 重新跑过 `scripts/style_anchor_check.py`，18x9 结果保持稳定：`02-list` 4.99%，`03-detail` 7.19%，`04-form` 3.32%，`05-completion` 2.62%，`06-loading` 2.85%，`07-profile` 3.47%，`08-modal` 3.38%。
- 这轮开始把全局设计系统 token 从锚点页里拆出来，落成 `--ds-*` 语义变量、基础按钮/卡片/徽标/列表/表单组件，以及一个可直接查看的 `#/design-system` 开发预览页。
- 设计系统页按窄屏重新收口后，颜色 token、排版、按钮、徽标、提示横幅、列表和表单区域都能在 430px 宽度里完整显示，不再出现标题副行被挤出视口的问题。
- `scripts/style_anchor_check.py` 复跑结果没有回退，说明新设计系统整理没有破坏既有 18x9 锚点页。
- 新底座页先接入了首页、反馈、旅客个人中心和司机个人中心四组骨架，后续新的业务页优先从这些 `ab-*` 组件继续扩展。
- 这轮又把司机站点详情里的场区地图标签补成了真正的胶囊样式，`#/driver/station/beijing-map` 现在能同时承载地图预览和两个区域入口，不再只是裸文本。
- 我在 Safari 里重新检查了首页、反馈、司机个人中心、车站排队、短途复载和司机站点地图页，新的 `ab-*` 底座在窄屏移动端和桌面预览下都保持了同一套密度和层级。
- 新 `ab-*` 业务页现在统一进入 `mobile-preview-app` 画布：桌面端按 `430 × 860`、`1:2` 手机比例固定预览，页面内容在画布内部滚动，底部导航固定在画布底部，避免被电脑浏览器视口拉成长页。后续又去掉了预览缩放的 `1x` 上限，大窗口里会按可视区等比放大，而不是停留在一个过小的 430px 窄条。
- 首页头图的外层网格项补了明确的最小高度，修掉了 `ab-page-body` 作为 grid 时头图外壳被压成二十几像素的裁切问题；同时把静态资源版本号再往上提了一档，防止 Safari 继续缓存旧的首页布局。
- 这轮把 `#/splash`、`#/station/select`、`#/station/switch` 也接进了同一套 `mobile-preview-app` 固定画布，开屏页和站点选择页不再继续走旧的自适应壳；同时把 `ab-info-row` 统一改成真正的左右分栏，解决多个列表卡片底部那条 `›` 跑到左下角的问题。
- 首页头图再收了一版：外层改成留白包裹的卡片容器，主视觉不再贴边，背景图位置也上移了一点，避免站点图像像被屏幕边缘硬切出来。
- 继续微调首页头图外边距：`ab-home-hero` 的左右留白已经收掉，让头图卡片和下面公告、服务区保持同一条内容边线，不再显得上面那张卡片偏窄。
- 2026-05-24 进入 v2 视觉校准和图标系统化阶段：已读取新的 Obsidian 工作流与参考文档，确认新增 `Style Recipe` 层，用少量校准页驱动全局 token/component/recipe 返修。
- 这轮把首页头图底部的白色收口渐变改成了更暗的黑色收口，并把“站区服务”从 8 张独立卡片改成单个分组卡片里的 8 个按钮；同时重新生成了 `docs/ui/icon-inventory.json`，把之前损坏的清单恢复成合法 JSON。
- 图标层先完成基础重构：`ICONS` 与 `ANCHOR_ICONS` 合并为统一 `ICON_LIBRARY`，`iconMarkup()` 和 `anchorIcon()` 都从同一个 SVG/currentColor 图标库输出。
- 已替换第一批明显语义误用图标：积分不再用扫码，邮箱不再用扫码，场站接驳不再复用公交，自驾停车不再复用小汽车，退出登录不再复用扫码。
- 新增 `docs/ui/icon-inventory.json`、`docs/ui/icon-style.md`、`docs/ui/style-recipes.yaml`、`docs/ui/visual-calibration/sample-set.yaml` 与 `docs/ui/visual-thresholds.json`，把图标清单、3x3 生图校准规则和页面 recipe 规则落盘。
- 新增 `scripts/icon_inventory.mjs` 用于扫描图标定义和使用情况；新增 `scripts/icon_sheet_tools.py` 用于 3x3 图标板切片、CSV alpha 模拟和像素 diff 检查。
- 图标重构后复跑锚点检查，MAE 结果保持稳定：`02-list` 4.97%，`03-detail` 7.18%，`04-form` 3.31%，`05-completion` 2.62%，`06-loading` 2.84%，`07-profile` 3.47%，`08-modal` 3.37%。
- 第一批 3x3 图标生图校准完成：覆盖 `pin/map/route/megaphone/search/user/taxi/car/transfer`，第二轮生图经阈值化后得到纯黑白校准板，路径为 `assets/icons/calibration/board-01-high-frequency/board-clean.png`。
- 第一批图标板已用 `scripts/icon_sheet_tools.py extract` 切片并生成 CSV 模拟图，`maxDiffRatio = 0.0`，满足 `diffRatio <= 5%`；记录落在 `docs/ui/icon-calibration-log.md`。
- 第一批图标已经进入 `#/design-system` 的“图标系统”集中预览区；业务页里最完整的查看入口是 `#/station/home`，能同时看到定位、公告、搜索、导航、交通、接驳、身份、出租车、网约车这些语义图标。
- 按用户反馈进一步校准图标：新增 `scripts/icon_svg_replica.py`，把九宫格拆成 9 张单独目标图和 3 倍放大图，再生成 `currentColor` SVG mask；第一批 9 个图标的单图复刻 `maxDiffRatio = 0.0`，页面优先从 `ICON_REPLICA_LIBRARY` 取这批图标，手写 `ICON_LIBRARY` 只作为兜底。
- 继续修正第一批复刻图标偏小的问题：页面 viewBox 改为按每个图标真实黑色线条的 `inkBBox` 裁边并加 6% padding，不再强行塞进统一方形裁框；浏览器负责在方形 icon 盒子里居中显示。
- 追加两处图标光学校正：`search` 图标统一右移 `1px`，修正斜柄造成的小尺寸视觉偏左；`car` 图标统一放大 `1.18`，修正横向车身放进方形 icon 盒后显小的问题。
- 继续补齐剩余高频图标：新增 `scripts/icon_svg_board.mjs`，把统一 `ICON_LIBRARY` 里的语义图标生成同规格 3x3 SVG 校准板，再用 Chrome headless 输出 PNG target。
- 第二批 `board-02-settings-feedback` 覆盖 `message/globe/accessibility/shield/lock/ear/feedback/phone/mail`，CSV 模拟 `maxDiffRatio = 0.007515`，SVG mask 复刻 `maxDiffRatio = 0.006313`，通过 5% 门槛。
- 第三批 `board-03-service-utility` 覆盖 `home/parking/points/calendar/clock/back/train/people/history`，CSV 模拟 `maxDiffRatio = 0.008287`，SVG mask 复刻 `maxDiffRatio = 0.007652`，通过 5% 门槛。
- `index.html` 现在按顺序加载三批 `icon-replicas-board-01/02/03.js`，`renderIcon()` 仍统一优先走 `ICON_REPLICA_LIBRARY`，再回落到基础 `ICON_LIBRARY`。
- `#/design-system` 的图标系统区按“第一批校准 / 设置反馈 / 服务工具”分组展示 27 个复刻图标；`docs/ui/icon-inventory.json` 刷新后显示 49 个已用语义、0 个缺失。
- 按用户指出“三批感觉不一样”和“线条再粗一些”，旧的第二、第三批 SVG target 已废弃；三批全部重新用加粗生图提示词生成 3x3 target，再阈值化成纯黑白校准板。
- 新加粗版三批重新跑完 CSV 模拟和单图 SVG mask 复刻，`board-01/02/03` 的两类检查 `maxDiffRatio` 均为 `0.0`；页面仍加载同一套 `ICON_REPLICA_LIBRARY`，只是内部形状换成粗线版本。
- 设计系统图标预览格收紧了列间距并补 `min-width: 0`，避免 430px 预览下第三列边缘被挤出。
- 用户指出业务页圆角和 18x9 风格锚点不一致后，重新确认差异来自画布比例：锚点页按 `864 × 1728` 绘制，业务页按 `430 × 860` 预览，所以 24px 锚点卡片圆角在业务页约等于 12px。
- 本轮把业务页圆角收敛进 `--ds-radius-*` token：控件 8px、小卡片 10px、标准卡片 12px、头图/媒体 14px、胶囊保持 999px；`ab-*` 卡片、面板、列表、表单、图标容器已迁移到这些 token，避免继续保留 16/18/20/26px 的过圆旧值。
- `#/design-system` 增加“圆角 Token”预览区，`docs/design-tokens.md` 与 `docs/ui/style-recipes.yaml` 同步记录了锚点到业务画布的圆角换算规则，后续新增页面应优先使用语义 token，而不是页面级硬编码圆角。
- 进一步从锚点页抽出上层密度规则：简单信息不再默认一条一个独立卡片，而是进入 `ab-info-list` cell-group，外层一个白色容器，内部行用分割线；标准 cell 56px，紧凑 cell 52px，section 间距收敛到 12px。
- 的士之家基本信息页修掉了旧组件混用问题：首行“北京西站的士之家”不再使用单独的 `ab-house-location-card`，改为和地址、开放时间、可容纳量同一组 `ab-info-row`，左侧图标、行高和左右内边距统一。
- 新增第四批 3x3 图标校准板 `board-04-taxi-house`，覆盖 `lounge/dining/charger/wifi/tea/book/medical/restroom/gift`，CSV 模拟与单图 SVG mask 复刻都为 `maxDiffRatio = 0.0`；司机端底栏和的士之家服务入口已接入这批图标。
- 重新定位个人中心等页面卡片底部 hover 白条：根因不是信息行 padding，而是固定高度 `mobile-preview-app` 中 `.ab-page-body` 作为 grid 时默认 `align-content: normal/stretch`，把页面剩余高度分摊到各个 auto track，导致 `ab-info-list` 被父级拉高、最后一行下方留下白色内部空白。已将 `.ab-page-body` 设为 `align-content: start`，实测基础设置卡片最后一行到容器底部的间隙从约 10.469px 收敛为 1px 边框。
- 本轮继续补齐图标系统：新增 `board-05-interface-controls`、`board-06-mobility-feedback`、`board-07-remaining-utility` 三批复刻板，把之前仍回落到手写 `ICON_LIBRARY` 的 `check/more/question/refresh/settings/edit/scan/camera/id/angry/bike/bus/chat/cup/glove/leaf/logout/paper/pillow/plane/grid/handshake/qr/thumb` 全部接入 `ICON_REPLICA_LIBRARY`。重新跑 `node scripts/icon_inventory.mjs` 后，60 个基础图标都有复刻 SVG mask，已用语义 0 缺失。
- 站区公告页的 `全部/紧急/通知/提示/活动` 已从“跳回同一路由”改为真正的筛选按钮：点击分类会写入 `state.selected.announcementCategory` 并过滤公告列表；`提示` 类别同时匹配 `12306实名核验系统升级提醒` 与 `行李托运服务操作流程更新` 两条公告。
- 按用户指出的流程偏差，`board-04` 到 `board-07` 已废弃旧的 SVG 目标板路径，重新走“生图模型生成 3x3 raw target -> `scripts/icon_clean_board.py` 清理成纯黑白 target -> CSV alpha 模拟 -> 单图 SVG mask 复刻”的前三批链路；四批 CSV 与 SVG mask 检查的 `maxDiffRatio` 均为 `0.0`。
- 站区公告筛选页收敛提示逻辑：只有“全部”分类保留顶部红色“当前有1条紧急公告”提醒，`紧急/通知/提示/活动` 分类页不再显示这张提醒卡，只展示筛选后的公告列表和底部最近 30 天说明。
- 根据教师补充口径，短途复载的加分和减分统一按 `10分` 计算；预约按钮、规则说明、顶部统计、行程记录、积分明细和司机个人中心积分演示值已统一到 `+10分 / -10分` 体系。
- 开屏页回到“单张全屏图可点击”的方式，不再叠加 HTML 统计卡；旅客端和司机端分别使用不同的随机开屏图池，旅客端为车站/高铁风格，司机端为出租车场站风格。
- 本轮把 `#/station/select` 与 `#/station/switch` 都重做成统一的横向滑动站点卡片：保留 10 个站点中文名，卡片内嵌站点建筑位图和站点轮廓图标，底部只保留已选站点摘要与确认按钮，不再出现原稿多余站名胶囊。
- 首页头图已改用 `assets/bitmap/hero/splash-hero.png` 这张高分辨率站点建筑图作为主视觉，图片直接贴合卡片左右与底边，底部继续用黑色收口保证白字可读。
- 导航指引页改为真实组件 + 位图素材：`3D`、`平面`、`AR` 使用 `assets/bitmap/navigation/crops/` 里的裁切图，`B1/F1/F2/F3` 点击后会同步改变楼层按钮、目标点、路线线条、路线说明和步行时间。
- 为修正“业务页比锚点页字小、部分字过粗”的问题，业务页 section 标题、topbar 标题、cell 主文案、表单/提示卡正文统一放大一档，同时把若干 `850/900` 的普通标题权重收回到 `800`，保留数值和首屏大标题的强调。
- 根据用户截图复核，`#/station/select` 首次进入时默认落在北京南站，`#/station/switch` 仍落在当前已选站点；选站页返回按钮已换成统一 `back` SVG 圆形按钮。
- 首页头图不再使用通用开屏图，而是跟随当前站点使用同一张站点卡片建筑图；站点卡片左下角已从旧 `P04_icon_*.png` 切到 `station_*` 复刻 SVG mask。
- 北京南站公告已做站点条件化：首页和 `#/announcements` 独立公告页在当前站点为北京南站时显示 `北京南站北广场...` 与 `地铁4号线...`，其它站点继续保持默认西站公告。
- 根据用户反馈“字儿又小又粗”，再次对业务页排版密度做了二次校准：业务页主文案从约 `14px/800` 提到约 `15.5px/600`，topbar 提到约 `20px/700`，section 标题提到约 `18px/700`，辅助文案统一约 `13px/400`；只保留首页主标题、天气、积分、排队数值等关键数字为 `800` 强调，并通过截图复查首页、公告、个人中心、短途复载、的士之家和站点选择页。
- 用户指出站点建筑图与真实站点差异过大后，重新按真实外观特征生成 10 个站点主图，并统一切出 `assets/bitmap/stations-v2/landscape/` 横版首页图和 `assets/bitmap/stations-v2/portrait/` 竖版选择卡片图；首页默认使用横图，站点选择/切换卡片使用竖图。复查时首都机场初稿偏大兴感，已重生为 T3 长条红金屋顶；大兴机场也重生为低空斜视的五指廊/凤凰放射结构。首页公告卡片移除了重复的“点击查看公告详情”副文案，只保留公告标题和右侧进入符号。
- 继续处理用户要求的站点 icon：上一版错误地从站点图直接算法提轮廓，已按用户纠正改为“站点图视觉参考 -> 生图模型重绘 3x3 icon target -> `scripts/icon_clean_board.py` 纯黑白清理 -> CSV 模拟 -> 单图 SVG mask 复刻”。`board-08-station-icons-a` 与 `board-09-station-icons-b` 两批覆盖 10 个站点，CSV 模拟和单图 SVG mask 复刻均为 `maxDiffRatio = 0.0`。`#/station/select`、`#/station/switch` 和 `#/design-system` 已加载这两批 `station_*` 图标，不再在页面里直接使用旧站点 PNG。
- 本轮修正两处截图反馈：的士之家顶部绿色密度卡补回全局卡片圆角，并把该页仍写成“北京西站”的司机服务中心、的士之家名称和地址改为“北京南站”；首页“站区服务”8 宫格把 tile 内容从顶部对齐改为居中对齐，并收紧上下 padding，让 icon 与文字在分隔线方格里视觉居中。
- 首页头图在底部黑色收口之外追加了左侧横向黑色遮罩，保证左侧站名和实时客流文字压在复杂建筑图上仍然清楚；首页公告标签胶囊单独缩小一档，避免“紧急/通知/提醒”抢走公告标题层级。
- 首页“站区服务”继续保留单卡片 4x2 分割宫格，但去掉每个 icon 背后的彩色圆角方块，改用更大的 currentColor 图标直接居中；“出行参考”从三张竖排小卡改成一个分组列表，出租车、网约车、地铁入口均为 icon 在左、文案在右的横向行。
- 继续按用户截图把旧 icon 底板系统性收掉：交通上车点、网约车卡片、综合交通操作卡、旅客/司机个人中心信息行、车站排队、短途复载目标车站、的士之家基本信息与服务项都统一改为无圆角底板的大号 SVG/currentColor 图标。短途复载目标车站同时由“北京西站”改为“北京南站”，并替换为 `station_south` 站点轮廓图标。
- 远端 `origin/main` 当前多出 `c02b148 Trim to runtime files for deployment`，该提交删除了 `assets/icons/calibration/.../icon-replicas-board-08.js` 和 `board-09.js` 等图标运行文件，但 `index.html` 仍会加载这些路径；已用 curl 复核 GitHub Pages 上站点 icon 脚本为 404。为避免部署只剩运行文件时站点 icon 消失，已把 10 个 `station_*` 站点图标定义内嵌到 `app.js` 作为运行时兜底。
- 首页“站区公告”三行公告只在首页列表里启用单行省略，不影响独立公告页；短途复载预约页移除顶部“短途复载规则”卡；的士之家基本信息与服务项、司机个人中心统计标签补充不换行约束，避免末尾两三个字掉到第二行。
- 导航指引页按用户反馈完成二次图像化：用生图模型生成 `B1/F1/F2/F3` 四张平面站内图、`3D 总览` 和 `AR 指引` 六张资产，落在 `assets/bitmap/navigation/v2/`。页面移除了地图预览图里的重复楼层浮条，也删掉了“平面站内图 · 出入口 / 服务设施 / 当前定位”这句说明；楼层切换只保留在“路线规划”卡片里，平面图会跟随当前楼层切换。
- 用户进一步指出 3D 页仍有两组楼层、图片横向偏小且风格偏重后，第三轮改为竖屏导航素材：`assets/bitmap/navigation/v3/` 作为当前运行资产；3D 图内不再放 `总览/B1/F1/F2/F3` 控件，只由图片本身表达四层分离，页面下方保留唯一一组路线规划楼层按钮。导航图片容器改成更高的竖向比例，并通过浅蓝灰底色、低阴影和统一滤镜压低图片的渲染感。
- 公告页顶部分类按参考图重构为全局 `ab-tab-row` 文本 tab：选中项为蓝色文字和短下划线，紧急项带红色数字徽标；这套样式已同步用于导航、交通、停车、司机排队、场站详情、短途复载和的士之家等业务顶部切换。公告搜索和分类最终保持同一白色工具区，不再使用卡片式筛选按钮。

## 2026-05-27

- 已读取用户新需求文档 `/Users/yuookie/Documents/ob一串字母/2026 冬新学期/2026-05-26/到站北京的新要求.md`，本轮按“每一步 Git 保存”拆分提交。
- 京通入口页替换为老师给出的清晰截图 `Screenshot_20260527_003533.jpg` 的压缩运行版，解决原入口页模糊问题。
- 老师给出的站点照片包只作为视觉资产来源使用，原始 zip 保持未提交；运行时站点图压缩到 `assets/bitmap/stations-v3/`，北京南站等首页/选站图都改用这批压缩 WebP。亦庄站在本次参考包中未见单独照片，暂沿用上一版站点图。
- 运行时开屏图池也从 PNG 切到压缩 WebP：旅客端两张约 81KB，司机端约 100KB/110KB；旧 PNG 作为历史源图暂不进入运行链路。
- `#/station/select` 首次选站页改为更紧凑的两列卡片，取消 `确认选择`，点选卡片直接进入首页；`#/station/switch` 仍保留 `确认更改`，并给底部确认区补了 safe-area padding。
- 首页 8 个入口按老师新顺序调整为 `站内导航 / 交通接驳 / 场站换乘 / 自驾停车 / 站区公告 / 站内服务 / 短途复载 / 投诉建议`，不再放 `个人中心`；底部导航改为 `首页 / 导航 / 交通 / 公告 / 个人`。
- 首页和公告里的 12306、售票机维护两条已替换为天气预警：`今日有暴雨蓝色预警，出门请注意带伞` 与 `今明两日大风橙色预警，出行请注意安全`。
- 新增 `#/station/services` 站内服务页和 `#/station/services/inquiry` 问询服务页。按老师文档保留 `李包托运、提取、寄存` 原文，后续如老师确认应为 `行包` 或其它写法再统一改。
- 图标没有做页面级零散加粗；基础业务图标已从 `board-01` 到 `board-07` 重新生成更粗的源 target，继续保持 `currentColor` 和统一图标库出口，`renderIcon()` 不再追加运行时描边。
- `#/nav/map` tab 顺序调整为 `平面 / 实景 / 3D`，`AR` 改名 `实景`；平面图默认只突出卫生间点位，新增 `出租车上车区` 与 `南广场出站口` 两个横向按钮，分别跳转 B1/F1 并显示对应标记，同时保留当前位置星标。
- 交通页 tab 调整为 `推荐 / 地铁 / 公交 / 出租 / 网约`，`综合` 改为 `推荐`；推荐页顶部新增目的地搜索框，默认 `北京大学`，提交后展示北京南站到北京大学的路线摘要。
- 地铁页改为北京南站真实线路口径，展示 4号线大兴线和 14号线；每张卡补发车间隔和 5 人客流指示，移除了购票按钮。发车间隔和静态拥挤度属于原型演示值，真实实时数据仍需后续接入或人工确认。
- 北京南站地铁线路口径已用京港地铁官方页面交叉确认：4号线-大兴线北京南站站点页 `https://www.mtr.bj.cn/service/line/station/5d5a18eeb1ea0278b8fffd71.html`，14号线北京南站站点页 `https://www.mtr.bj.cn/service/line/station/5d5a13a4b1ea0278b8fffd0b.html`，14号线站间距页也列有 `景风门 / 北京南站 / 陶然桥`。
- 场站换乘入口不再进入普通交通页，新增 `#/station-transfer/select` 和 9 个目的地路线页；截图中 `顺风车` 已统一替换为 `网约车`，`打车` 替换为 `出租`，发车间隔去掉 `首站发车约` 前缀。
- 停车页新增 `立体停车场`，停车场数量改为 5 个；列表空位数和进度条重新对齐，锦州街价格改为 `1小时 4.5 / 3小时 13.5 / 全天封顶 50`，立体停车场价格为 `1小时 18 / 3小时 54 / 全天封顶 100`。
- 430px 手机视口自动回归时发现站区切换页确认条会落到可视区外，已把移动预览壳高度收敛到 `100dvh/100vh` 内，确认条现在能落在可视范围内。
- 按用户反馈继续重做非站点图标：`board-01` 到 `board-07` 全部重新走“生图源 target -> 纯黑白清理 -> CSV 模拟 -> 单图 SVG mask 复刻”链路，站点图标 `board-08/09` 不重生。提示词采用自包含描述，强调 plain/sparse/geometric/low-detail 和 1024px 图板约 26px 粗线；遇到模型漂向复杂风格时优先通过不提及触发词收回，而不是堆叠禁止项。所有七批 CSV 与 SVG mask 检查均为 `maxDiffRatio = 0.0`。
- 继续按用户反馈放大页面图标：全局 icon token 和首页服务格、信息行、底栏、交通/换乘等主要组件的 SVG 显示尺寸整体放大一档；`board-01` 的第 9 个 `transfer` 图标单独换回 iteration-05 / Image #1 的右下角版本，重新生成 CSV 与单图 SVG mask 后仍为 `maxDiffRatio = 0.0`。
- 交通接驳页重构前先补齐第十批 3x3 图标板 `board-10-traffic-routing`：一次性生成 `walk/metro/traffic_bus/traffic_taxi/ride_hailing/route_swap/filter_sliders/departure_time/route_recommend`，人工审核后落成 `currentColor` SVG mask；CSV 与 SVG mask 检查均为 `maxDiffRatio = 0.0`，并已接入运行时和设计系统预览。
- `#/traffic/mixed` 顶部搜索改为未搜索时复用导航页式单行搜索，提交后展开为起终点卡；绿点和红点之间补虚线，左上增加纯 chevron 返回未搜索态。推荐页移除“最后更新 / 数据实时 / 交通方式推荐”正文层级，地铁、出租、公交、网约改成统一路线方案卡，每条都接入语义图标；高德式步行段改为紧裁剪的实心 `walk_solid` 小人图标 + 数字胶囊，不再显示 `步1/步3` 文本。
- 按用户首页 UI 微调反馈，`#/station/home` 头图卡片高度从 258px 收到 232px；`ab-info-list` 内部长条行不再显示横向分割线；首页公告 `紧急/提示/通知` 标签高度从 21px 收到 18px，降低胶囊存在感。
- 待人工复核项：地铁拥挤度/发车间隔若要实时准确，需要明确数据源；老师是否要求“重新生图”而不是直接压缩参考照片，需要下一轮视觉反馈确认；真机浏览器仍建议最终走一遍。

## 设计判断

- 保留原稿的信息层级：入口页 -> 旅客端/司机端 -> 站点选择 -> 各服务页。
- 重构视觉语言：用更克制的政务交通服务风格替代原图的大渐变、大阴影、大圆角和松散卡片。
- 旅客端主色使用蓝色，司机端保留绿色/蓝色区分，但全局组件密度、圆角、阴影保持一致。
- 后续真正做视觉重构时，先以当前截图热区原型校准页面和文案，再替换为设计系统组件，不能直接凭记忆重写文案。
